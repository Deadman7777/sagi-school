name: Build SAGI SCHOOL

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-linux:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Angular CLI
        run: npm install -g @angular/cli

      - name: Install frontend deps
        working-directory: frontend
        run: npm install

      - name: Build Angular
        working-directory: frontend
        run: ng build --configuration production

      - name: Install Electron deps
        working-directory: electron
        run: npm install

      # La version du .deb/.AppImage vient de electron/package.json :
      # on la force depuis le tag pour garantir la cohérence du nom
      - name: Version = tag git
        if: startsWith(github.ref, 'refs/tags/')
        working-directory: electron
        shell: bash
        run: npm pkg set version="${GITHUB_REF_NAME#v}"

      - name: Build Linux
        working-directory: electron
        run: npm run build:linux
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v4
        with:
          name: sagi-school-linux
          path: |
            dist-app/*.deb
            dist-app/*.AppImage

  build-windows:
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Validate Python requirements
        working-directory: backend
        run: pip install -r requirements/base.txt --quiet

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Angular CLI
        run: npm install -g @angular/cli

      - name: Install frontend deps
        working-directory: frontend
        run: npm install

      - name: Build Angular
        working-directory: frontend
        run: ng build --configuration production

      - name: Install Electron deps
        working-directory: electron
        run: npm install

      # La version du .exe vient de electron/package.json : on la force
      # depuis le tag pour garantir la cohérence du nom (oubli fréquent)
      - name: Version = tag git
        if: startsWith(github.ref, 'refs/tags/')
        working-directory: electron
        shell: bash
        run: npm pkg set version="${GITHUB_REF_NAME#v}"

      - name: Build Windows
        working-directory: electron
        run: npm run build:win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: "false"

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: sagi-school-windows
          path: dist-app/*.exe

  release:
    needs: [build-linux, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download Linux artifacts
        uses: actions/download-artifact@v4
        with:
          name: sagi-school-linux
          path: artifacts

      - name: Download Windows artifacts
        uses: actions/download-artifact@v4
        with:
          name: sagi-school-windows
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: artifacts/*
          body: |
            ## SAGI SCHOOL ${{ github.ref_name }}
            Telechargements disponibles ci-dessous.
            Windows  - SAGI SCHOOL Setup.exe
            Linux    - sagi-school.deb
            Linux    - SAGI SCHOOL.AppImage
            Installation Windows - telecharger Setup.exe et suivre l'assistant.
            Installation Linux - sudo dpkg -i sagi-school_*.deb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
